---
layout: layouts/base
---
<article class="poem">
  <header class="poem-header">
    <h1 class="poem-title">{{ title }}</h1>
    <p class="poem-date">{{ date | formatDate }}</p>
    {% if tags and tags | length %}
    <ul class="poem-tags">
      {% for tag in tags %}
      <li class="poem-tag">{{ tag }}</li>
      {% endfor %}
    </ul>
    {% endif %}
  </header>

  <div class="poem-body">
    {{ content | safe }}
  </div>

  {% if commit_note %}
  <footer class="poem-footer">
    <hr class="poem-rule" />
    <p class="poem-commit-note">{{ commit_note }}</p>
  </footer>
  {% endif %}

  <nav class="poem-nav">
    <a href="{{ '/' | url }}" class="poem-back">← All poems</a>
  </nav>
</article>
